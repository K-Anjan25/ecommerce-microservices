# Vite only exposes variables prefixed with VITE_. Keep this empty when the
# frontend is served by the API gateway proxy so browser requests stay same-origin.
VITE_API_BASE_URL=

# Public Key ID ONLY (never put the secret here):
VITE_RAZORPAY_KEY_ID=rzp_test_TTzOy9g73a5u5A
